(define println (fn [msg] (print msg "\n")))
(define var [1 2 3 4 5])
(println (get var 2))
(println (len var))
(define each (fn [i values]
  (println (get values i))
  (if (= (+ i 1) (len values)) 0 (each (+ i 1) values))))
(define each-variadic (fn [i values...] (each 0 values)))
(each 0 var)
(each-variadic 0 1 2 3 4 5)
(println (get var 1000))
